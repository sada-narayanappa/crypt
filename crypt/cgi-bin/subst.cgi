#!/usr/bin/perl -w
use strict;
use myUtils;
use CGI qw(:standard);
use CGI::Carp;
#-------------------------------------------------------------------------------
# prototypes
sub main();
my $opt_v = 0;
my $opt_d = 0;
#-------------------------------------------------------------------------------
main();
my $DOCROOT="html";
#-------------------------------------------------------------------------------
sub main()
{
	myHeader();
	doget("html/subst.html");
	myFooter();
}
#-------------------------------------------------------------------------------
sub doget()
{
	my ($file) = @_;
	my $resp  = new CGI;
}
#-------------------------------------------------------------------------------
